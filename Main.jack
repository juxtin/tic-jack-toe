class Main {
  function void main(){
    var Board board;
    var int location;
    var boolean x;
    
    let board = Board.new();

    let x = false;
    while(true){
      let location = board.chooseMove();
      do board.move(location, x);
      let x = ~x;
    }

    return;
  }

}
